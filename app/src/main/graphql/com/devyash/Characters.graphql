query ExampleQuery($page: Int) {
    characters(page: $page) {
        results {
            image
            name
            origin {
                name
            }
            species
            location {
                name
            }
        }
    }
}